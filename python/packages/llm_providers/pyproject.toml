[project]
name = "llm-providers"
version = "0.1.0"
description = "Provider abstraction for LLM calls"
requires-python = ">=3.12"
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/llm_providers"]
