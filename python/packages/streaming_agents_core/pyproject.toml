[project]
name = "streaming-agents-core"
version = "0.1.0"
description = "Domain models, scoring utils, and shared logic"
requires-python = ">=3.12"
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/streaming_agents_core"]
