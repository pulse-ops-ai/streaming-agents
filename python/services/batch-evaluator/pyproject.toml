[project]
name = "batch-evaluator"
version = "0.1.0"
description = "Offline scoring and backtests"
requires-python = ">=3.12"
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/batch_evaluator"]
