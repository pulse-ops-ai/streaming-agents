# 030 – Python Standards

- Use **uv** as the package manager (`pyproject.toml` at `python/`)
- Python ≥ 3.12
- Formatter: **Ruff** (`ruff format` + `ruff check`)
- Type hints required on all public functions
