# Default configuration for edge exporter IAM user
# Override these values as needed for different environments

aws_region  = "us-east-1"
aws_profile = "streaming-agents-sandbox-kong"
stream_name = "streaming-agents-r17-telemetry"
environment = "dev"

# Lex bot configuration for voice terminal
# Specific bot ID for the streaming-agents Lex bot
lex_bot_id = "XXXXXXXXXX"
